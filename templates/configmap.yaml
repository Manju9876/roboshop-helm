apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.component_name }}
data:
#  {{- include ".Values.configmap_data" . | indent 2 }}
  {{- range $key, $value := .Values.configmap_data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
